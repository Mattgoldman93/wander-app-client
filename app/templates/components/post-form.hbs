<h3>Create a New Post</h3>

<form {{action 'createPost' on='submit'}}>
{{input class="content-input" placeholder="Post content goes here" value=newPost.content}}
{{input class="attachment-input" type="file" value=newPost.attachment}}
  <button type="submit">Save that</button>
</form>
